import { ModelNames } from './model-names.constant';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const EsModelNames = {
  ADMIN: ModelNames.ADMIN.toLowerCase(),
  AREA: ModelNames.AREA.toLowerCase(),
  CITY: ModelNames.CITY.toLowerCase(),
  APPOINTMENT: ModelNames.BASE_APPOINTMENT.toLowerCase(),
  BRANCH_SERVICE_TYPE: ModelNames.BRANCH_SERVICE_TYPE.toLowerCase(),
  COMMENT: ModelNames.COMMENT.toLowerCase(),
  COMMENT_REPLY: ModelNames.COMMENT_REPLY.toLowerCase(),
  COUNTRY: ModelNames.COUNTRY.toLowerCase(),
  DYNAMIC_LINK: ModelNames.DYNAMIC_LINK.toLowerCase(),
  EVENT_CATEGORY: ModelNames.EVENT_CATEGORY.toLowerCase(),
  EVENT_FACILITY: ModelNames.EVENT_FACILITY.toLowerCase(),
  LOST_POST: ModelNames.LOST_POST.toLowerCase(),
  FOUND_POST: ModelNames.FOUND_POST.toLowerCase(),
  PET_BREED: ModelNames.PET_BREED.toLowerCase(),
  PET_FOLLOW: ModelNames.PET_FOLLOW.toLowerCase(),
  PET_TYPE: ModelNames.PET_TYPE.toLowerCase(),
  PET: ModelNames.PET.toLowerCase(),
  POST: ModelNames.POST.toLowerCase(),
  SERVICE_PROVIDER: ModelNames.SERVICE_PROVIDER.toLowerCase(),
  BASE_BRANCH: ModelNames.BASE_BRANCH.toLowerCase(),
  USER_FOLLOW: ModelNames.USER_FOLLOW.toLowerCase(),
  USER_PUSH_NOTIFICATION: ModelNames.USER_PUSH_NOTIFICATION.toLowerCase(),
  USER_SEGMENT: ModelNames.USER_SEGMENT.toLowerCase(),
  USER: ModelNames.USER.toLowerCase(),
};
